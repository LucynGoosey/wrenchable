name: Wrenchable
version: '${project.version}'
main: io.github.rypofalem.wrenchable.Wrenchable
api-version: 1.20
prefix: wrenchable
authors: [ rypofalem ]
description: '${project.description}'
commands:
  wrenchable:
    description: Your description
    usage: "/<command> dump - dump a list of whitelistable blocks to plugins/wrenchable/dump.yml"
    permission: op
